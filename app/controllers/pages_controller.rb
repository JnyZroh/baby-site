class PagesController < ApplicationController
  def home
    # @guests = Guest.all
    # @guest = Guest.find_by(phone_number: params[:phone_number])
    # if @guest.nil?
    #   flash[:alert] = "That phone number wasn't found, contact the host if this is a mistake."
    #   render 'home'
    # else
      # redirect_to edit_guest_path(@guest)
    # if @guest.nil?
    # end

    # end
  end

  def questions

  end


end
